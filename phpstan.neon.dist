parameters:
    level: 9
    paths:
        - src/
        - tests/
    tmpDir: .phpstan-cache
    bootstrapFiles:
        - vendor/autoload.php
    ignoreErrors:
        # Doctrine assigns entity id on persist via reflection
        - identifier: property.unusedType
          path: src/Entity/*.php
